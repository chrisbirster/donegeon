INSERT INTO tasks (
    id,
    content,
    description,
    project_id,
    section_id,
    sort_order,
    recurrence_rule,
    priority,
    due_text,
    due_deadline,
    checked,
    is_deleted,
    created_at,
    updated_at
) VALUES (
    :id,
    :content,
    :description,
    :project_id,
    :section_id,
    :sort_order,
    :recurrence_rule,
    :priority,
    :due_text,
    :due_deadline,
    0,
    0,
    :created_at,
    :updated_at
);
