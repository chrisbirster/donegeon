{
  "name": "donegeon-web",
  "private": true,
  "packageManager": "bun@1.2.5",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test:e2e": "turbo run test:e2e --filter=@donegeon/client",
    "test:e2e:ui": "turbo run test:e2e:ui --filter=@donegeon/client"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
